AWSTemplateFormatVersion: '2010-09-09'
Description: photo-uploader CodePipeline - EventBridge trigger and CodeDeploy hand-off

Parameters:
  ProjectName:
    Type: String

  ArtifactBucketName:
    Type: String

  PipelineName:
    Type: String

  AlertTopicArn:
    Type: String

  ConnectionArn:
    Type: String
    Description: An AVAILABLE CodeConnections connection.

  GitHubOrg:
    Type: String

  GitHubAppRepo:
    Type: String

  GitHubBranch:
    Type: String
    Description: The branch supplying appspec.yaml and taskdef.json.

  PipelineRoleArn:
    Type: String

  EventBridgeRoleArn:
    Type: String

  CodeBuildRoleArn:
    Type: String
    Description: Role the render stage runs as.

  EcrRepositoryName:
    Type: String

  TriggerImageTag:
    Type: String

  CodeDeployApplicationName:
    Type: String

  CodeDeployDeploymentGroupName:
    Type: String

Resources:

  # --- Artifact store -------------------------------------------------------

  ArtifactBucket:
    Type: AWS::S3::Bucket
    # Delete, not Retain: nothing here is precious. make empty clears it first.
    DeletionPolicy: Delete
    Properties:
      BucketName: !Ref ArtifactBucketName
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
            BucketKeyEnabled: true
      LifecycleConfiguration:
        Rules:
          # Without this the bucket grows a couple of megabytes per deployment.
          - Id: expire-artifacts
            Status: Enabled
            ExpirationInDays: 30
          - Id: abort-incomplete-uploads
            Status: Enabled
            AbortIncompleteMultipartUpload:
              DaysAfterInitiation: 7

  # The other two buckets refuse plaintext requests; this one should too.
  ArtifactBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref ArtifactBucket
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Sid: DenyInsecureTransport
            Effect: Deny
            Principal: '*'
            Action: 's3:*'
            Resource:
              - !GetAtt ArtifactBucket.Arn
              - !Sub '${ArtifactBucket.Arn}/*'
            Condition:
              Bool:
                'aws:SecureTransport': false

  # --- Render stage ---------------------------------------------------------

  RenderLogGroup:
    Type: AWS::Logs::LogGroup
    DeletionPolicy: Delete
    Properties:
      LogGroupName: !Sub '/aws/codebuild/${ProjectName}-render'
      RetentionInDays: 14

  # CodeDeployToECS substitutes only IMAGE1_NAME, so anything else the task
  # definition needs has to be literal by the time it reaches the deploy action.
  # This resolves it here, from Parameter Store, rather than in the repository.
  RenderProject:
    Type: AWS::CodeBuild::Project
    Properties:
      Name: !Sub '${ProjectName}-render'
      Description: Renders deploy/taskdef.json from its template
      ServiceRole: !Ref CodeBuildRoleArn
      Artifacts:
        Type: CODEPIPELINE
      Source:
        Type: CODEPIPELINE
        BuildSpec: deploy/buildspec.yml
      Environment:
        Type: LINUX_CONTAINER
        ComputeType: BUILD_GENERAL1_SMALL
        Image: aws/codebuild/amazonlinux2-x86_64-standard:5.0
        EnvironmentVariables:
          # The only input. Every name in the template derives from it.
          - Name: PROJECT
            Type: PLAINTEXT
            Value: !Ref ProjectName
      LogsConfig:
        CloudWatchLogs:
          Status: ENABLED
          GroupName: !Ref RenderLogGroup
      TimeoutInMinutes: 10

  # --- Pipeline -------------------------------------------------------------

  Pipeline:
    Type: AWS::CodePipeline::Pipeline
    Properties:
      Name: !Ref PipelineName
      PipelineType: V2
      ExecutionMode: QUEUED
      RoleArn: !Ref PipelineRoleArn
      ArtifactStore:
        Type: S3
        Location: !Ref ArtifactBucket
      Stages:
        - Name: Source
          Actions:
            # Produces imageDetail.json, carrying the digest that gets deployed.
            - Name: Image
              RunOrder: 1
              ActionTypeId:
                Category: Source
                Owner: AWS
                Provider: ECR
                Version: '1'
              Configuration:
                RepositoryName: !Ref EcrRepositoryName
                ImageTag: !Ref TriggerImageTag
              OutputArtifacts:
                - Name: ImageArtifact

            - Name: DeployConfig
              RunOrder: 1
              ActionTypeId:
                Category: Source
                Owner: AWS
                Provider: CodeStarSourceConnection
                Version: '1'
              Configuration:
                ConnectionArn: !Ref ConnectionArn
                FullRepositoryId: !Sub '${GitHubOrg}/${GitHubAppRepo}'
                BranchName: !Ref GitHubBranch
                # Only an image push starts a deployment, not a README edit.
                DetectChanges: false
              OutputArtifacts:
                - Name: ConfigArtifact

        - Name: Render
          Actions:
            - Name: TaskDefinition
              ActionTypeId:
                Category: Build
                Owner: AWS
                Provider: CodeBuild
                Version: '1'
              InputArtifacts:
                - Name: ConfigArtifact
              OutputArtifacts:
                - Name: RenderedArtifact
              Configuration:
                ProjectName: !Ref RenderProject

        - Name: Deploy
          Actions:
            - Name: BlueGreen
              ActionTypeId:
                Category: Deploy
                Owner: AWS
                Provider: CodeDeployToECS
                Version: '1'
              InputArtifacts:
                - Name: RenderedArtifact
                - Name: ImageArtifact
              Configuration:
                ApplicationName: !Ref CodeDeployApplicationName
                DeploymentGroupName: !Ref CodeDeployDeploymentGroupName
                TaskDefinitionTemplateArtifact: RenderedArtifact
                TaskDefinitionTemplatePath: deploy/taskdef.json
                AppSpecTemplateArtifact: RenderedArtifact
                AppSpecTemplatePath: deploy/appspec.yaml
                # Substitutes the digest into IMAGE1_NAME in taskdef.json.
                Image1ArtifactName: ImageArtifact
                Image1ContainerName: IMAGE1_NAME

  # --- Triggers and notifications -------------------------------------------

  EcrPushRule:
    Type: AWS::Events::Rule
    Properties:
      Name: !Sub '${ProjectName}-ecr-push'
      Description: Starts the deployment pipeline when a new image lands
      State: ENABLED
      EventPattern:
        source:
          - aws.ecr
        detail-type:
          - ECR Image Action
        detail:
          action-type:
            - PUSH
          result:
            - SUCCESS
          repository-name:
            - !Ref EcrRepositoryName
          # :latest is the only tag, so this matches the one event a build emits.
          image-tag:
            - !Ref TriggerImageTag
      Targets:
        - Id: codepipeline
          Arn: !Sub 'arn:${AWS::Partition}:codepipeline:${AWS::Region}:${AWS::AccountId}:${Pipeline}'
          RoleArn: !Ref EventBridgeRoleArn

  PipelineFailureRule:
    Type: AWS::Events::Rule
    Properties:
      Name: !Sub '${ProjectName}-pipeline-failed'
      Description: Notify when a pipeline execution fails
      State: ENABLED
      EventPattern:
        source:
          - aws.codepipeline
        detail-type:
          - CodePipeline Pipeline Execution State Change
        detail:
          pipeline:
            - !Ref Pipeline
          state:
            - FAILED
      Targets:
        - Id: alert
          Arn: !Ref AlertTopicArn
          InputTransformer:
            InputPathsMap:
              pipeline: '$.detail.pipeline'
              execution: '$.detail.execution-id'
            InputTemplate: '"Deployment FAILED. Pipeline: <pipeline> Execution: <execution>"'

Outputs:
  PipelineName:
    Value: !Ref Pipeline
